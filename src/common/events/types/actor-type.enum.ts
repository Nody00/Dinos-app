export enum ActorType {
  SYSTEM = 'SYSTEM', // Automated system operations (cron jobs, background tasks)
  USER = 'USER', // User-initiated operations (user clicked button)
  API = 'API', // External API/integration operations
}
